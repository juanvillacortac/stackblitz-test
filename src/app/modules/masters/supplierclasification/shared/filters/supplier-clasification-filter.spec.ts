import { SupplierClasificationFilter } from './supplier-clasification-filter';

describe('SupplierClasificationFilter', () => {
  it('should create an instance', () => {
    expect(new SupplierClasificationFilter()).toBeTruthy();
  });
});
