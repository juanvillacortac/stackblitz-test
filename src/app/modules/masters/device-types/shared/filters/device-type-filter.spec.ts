import { DeviceTypeFilter } from './device-type-filter';
//coment
describe('DeviceTypeFilter', () => {
  it('should create an instance', () => {
    expect(new DeviceTypeFilter()).toBeTruthy();
  });
});
