import { DescriptionTypeFilter } from './descriptionType-filter';

describe('DescriptionType', () => {
  it('should create an instance', () => {
    expect(new DescriptionTypeFilter()).toBeTruthy();
  });
});
