import { UseofpackagingFilter } from './useofpackaging-filter';

describe('Useofpackaging', () => {
  it('should create an instance', () => {
    expect(new UseofpackagingFilter()).toBeTruthy();
  });
});
