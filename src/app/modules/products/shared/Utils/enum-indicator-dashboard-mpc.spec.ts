import { EnumIndicatorMPC } from './enum-indicator-dashboard-mpc';
describe('EnumIndicatorMPC', () => {
  it('should create an instance', () => {
    expect(new EnumIndicatorMPC()).toBeTruthy();
  });
})