import { PurchaselistViewmodel } from './purchaselist-viewmodel';

describe('PurchaselistViewmodel', () => {
  it('should create an instance', () => {
    expect(new PurchaselistViewmodel()).toBeTruthy();
  });
});
