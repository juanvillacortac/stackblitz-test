import { ListproductsViewmodel } from './listproducts-viewmodel';

describe('ListproductsViewmodel', () => {
  it('should create an instance', () => {
    expect(new ListproductsViewmodel()).toBeTruthy();
  });
});
