import { SupplierempViewmodel } from './suppliersemp-viewmodel';

describe('SupplierViewmodel', () => {
  it('should create an instance', () => {
    expect(new SupplierempViewmodel()).toBeTruthy();
  });
});
