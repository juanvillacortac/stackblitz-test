import { ProviderTimelineFilter } from './provider-timeline-filter';

describe('ProviderTimelineFilter', () => {
  it('should create an instance', () => {
    expect(new ProviderTimelineFilter()).toBeTruthy();
  });
});
