import { FilterPurchaseOrder } from './filter-purchase-order';

describe('FilterPurchaseOrder', () => {
  it('should create an instance', () => {
    expect(new FilterPurchaseOrder()).toBeTruthy();
  });
});
