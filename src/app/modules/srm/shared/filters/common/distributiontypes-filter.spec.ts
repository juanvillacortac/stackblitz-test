import { DistributiontypesFilter } from './distributiontypes-filter';

describe('DistributiontypesFilter', () => {
  it('should create an instance', () => {
    expect(new DistributiontypesFilter()).toBeTruthy();
  });
});
