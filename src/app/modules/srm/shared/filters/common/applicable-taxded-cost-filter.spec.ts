import { ApplicableTaxdedCostFilter } from './applicable-taxded-cost-filter';

describe('ApplicableTaxdedCostFilter', () => {
  it('should create an instance', () => {
    expect(new ApplicableTaxdedCostFilter()).toBeTruthy();
  });
});
