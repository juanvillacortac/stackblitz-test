import { NumbersordersFilter } from './numbersorders-filter';

describe('NumbersordersFilter', () => {
  it('should create an instance', () => {
    expect(new NumbersordersFilter()).toBeTruthy();
  });
});
