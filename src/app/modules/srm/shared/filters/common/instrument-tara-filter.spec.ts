import { InstrumentTaraFilter } from './instrument-tara-filter';

describe('InstrumentTaraFilter', () => {
  it('should create an instance', () => {
    expect(new InstrumentTaraFilter()).toBeTruthy();
  });
});
