import { TransportClassificationFilter } from './transport-classification-filter';

describe('TransportClassificationFilter', () => {
  it('should create an instance', () => {
    expect(new TransportClassificationFilter()).toBeTruthy();
  });
});
