import { DocumentTypeFilter } from './document-type-filter';

describe('DocumentTypeFilter', () => {
  it('should create an instance', () => {
    expect(new DocumentTypeFilter()).toBeTruthy();
  });
});
