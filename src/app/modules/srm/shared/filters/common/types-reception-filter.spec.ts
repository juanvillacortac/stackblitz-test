import { TypesReceptionFilter } from './types-reception-filter';

describe('TypesReceptionFilter', () => {
  it('should create an instance', () => {
    expect(new TypesReceptionFilter()).toBeTruthy();
  });
});
