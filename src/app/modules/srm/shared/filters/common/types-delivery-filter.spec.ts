import { TypesDeliveryFilter } from './types-delivery-filter';

describe('TypesDeliveryFilter', () => {
  it('should create an instance', () => {
    expect(new TypesDeliveryFilter()).toBeTruthy();
  });
});
