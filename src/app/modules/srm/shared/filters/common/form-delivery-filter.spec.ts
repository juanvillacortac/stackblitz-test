import { FormDeliveryFilter } from './form-delivery-filter';

describe('FormDeliveryFilter', () => {
  it('should create an instance', () => {
    expect(new FormDeliveryFilter()).toBeTruthy();
  });
});
