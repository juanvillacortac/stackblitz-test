import { FilterPurchaseOrderModal } from './filter-purchase-order-modal';

describe('FilterPurchaseOrderModal', () => {
  it('should create an instance', () => {
    expect(new FilterPurchaseOrderModal()).toBeTruthy();
  });
});
