import { FilterViewerocSupplier } from './filter-vieweroc-supplier';

describe('FilterViewerocSupplier', () => {
  it('should create an instance', () => {
    expect(new FilterViewerocSupplier()).toBeTruthy();
  });
});
