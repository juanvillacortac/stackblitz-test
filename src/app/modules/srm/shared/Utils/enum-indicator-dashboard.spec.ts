import { EnumIndicatorSRM } from './enum-indicator-dashboard';
describe('EnumIndicatorSRM', () => {
  it('should create an instance', () => {
    expect(new EnumIndicatorSRM()).toBeTruthy();
  });
});
