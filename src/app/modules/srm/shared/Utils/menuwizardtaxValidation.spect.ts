import { MenuwizardTaxValidation } from "./menuwizardtaxvalidation";

describe('MenuwizardTaxValidation', () => {
    it('should create an instance', () => {
      expect(new MenuwizardTaxValidation()).toBeTruthy();
    });
  });