import { AdjustmentTypeFilter } from './adjustment-type-filter';

describe('AdjustmentTypeFilter', () => {
  it('should create an instance', () => {
    expect(new AdjustmentTypeFilter()).toBeTruthy();
  });
});
