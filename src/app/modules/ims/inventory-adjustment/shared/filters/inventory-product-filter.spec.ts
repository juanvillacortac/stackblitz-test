import { InventoryProductFilter } from './inventory-product-filter';

describe('InventoryProductFilter', () => {
  it('should create an instance', () => {
    expect(new InventoryProductFilter()).toBeTruthy();
  });
});
