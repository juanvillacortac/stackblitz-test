import { InventoryProductAbc } from './inventory-product-abc';

describe('InventoryProductAbc', () => {
  it('should create an instance', () => {
    expect(new InventoryProductAbc()).toBeTruthy();
  });
});
