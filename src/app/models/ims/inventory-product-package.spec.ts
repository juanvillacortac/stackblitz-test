import { InventoryProductPackage } from './inventory-product-package';

describe('InventoryProductPackage', () => {
  it('should create an instance', () => {
    expect(new InventoryProductPackage()).toBeTruthy();
  });
});
