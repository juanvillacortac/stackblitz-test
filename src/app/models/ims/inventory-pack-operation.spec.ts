import { InventoryPackOperation } from './inventory-pack-operation';

describe('InventoryPackOperation', () => {
  it('should create an instance', () => {
    expect(new InventoryPackOperation()).toBeTruthy();
  });
});
