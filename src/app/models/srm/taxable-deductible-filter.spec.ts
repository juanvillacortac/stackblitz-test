import { TaxableDeductibleFilter } from './taxable-deductible-filter';

describe('TaxableDeductibleFilter', () => {
  it('should create an instance', () => {
    expect(new TaxableDeductibleFilter()).toBeTruthy();
  });
});
