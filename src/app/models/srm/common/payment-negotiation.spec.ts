import { PaymentNegotiation } from './payment-negotiation';

describe('PaymentNegotiation', () => {
  it('should create an instance', () => {
    expect(new PaymentNegotiation()).toBeTruthy();
  });
});
