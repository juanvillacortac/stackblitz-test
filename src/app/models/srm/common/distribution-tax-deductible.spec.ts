import { DistributionTaxDeductible } from './distribution-tax-deductible';

describe('DistributionTaxDeductible', () => {
  it('should create an instance', () => {
    expect(new DistributionTaxDeductible()).toBeTruthy();
  });
});
