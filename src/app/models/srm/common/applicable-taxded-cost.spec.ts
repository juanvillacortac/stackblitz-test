import { ApplicableTaxdedCost } from './applicable-taxded-cost';

describe('ApplicableTaxdedCost', () => {
  it('should create an instance', () => {
    expect(new ApplicableTaxdedCost()).toBeTruthy();
  });
});
