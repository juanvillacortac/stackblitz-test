import { TransportClassification } from './transport-classification';

describe('TransportClassification', () => {
  it('should create an instance', () => {
    expect(new TransportClassification()).toBeTruthy();
  });
});
