import { Typedocumentoperation } from './typedocumentoperation';

describe('Typedocumentoperation', () => {
  it('should create an instance', () => {
    expect(new Typedocumentoperation()).toBeTruthy();
  });
});
