import { TypedocumentoperationFilter } from './typedocumentoperation-filter';

describe('TypedocumentoperationFilter', () => {
  it('should create an instance', () => {
    expect(new TypedocumentoperationFilter()).toBeTruthy();
  });
});
